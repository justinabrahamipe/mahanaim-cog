import { SisterChurch } from '@/types';
import churchesData from '../../churches.json';

// Edit churches.json (project root) to update sister church details.
export const churches: SisterChurch[] = churchesData;
